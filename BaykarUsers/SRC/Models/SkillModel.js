class SkillModel {
    constructor(id, userId, skillName, skillLevel) {
      this.id = id;
      this.userId = userId;
      this.skillName = skillName;
      this.skillLevel = skillLevel;
    }
  }
  
  export default SkillModel;